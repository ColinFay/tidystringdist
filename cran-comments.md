## Resubmission

This is a resubmission of 0.1.5 (rejected by CRAN incoming pre-test on
2026-05-04 with "possibly invalid URLs", all reported as 301 Moved
Permanently).

In this version (0.1.6) the offending URLs have been removed:

- The codecov and travis-ci badges have been dropped from the README.
- The auto-generated `tests_and_coverage` vignette (built by covrpage)
  has been removed entirely, along with its source artifact in
  `tests/README.md`

## R CMD check results

0 errors | 0 warnings | 1 note (only "unable to verify current time",
unrelated to the package).
